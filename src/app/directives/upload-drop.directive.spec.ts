import { UploadDropDirective } from './upload-drop.directive';

describe('UploadDropDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadDropDirective();
    expect(directive).toBeTruthy();
  });
});
